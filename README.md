# server-stat-script
## server-stat-script is a script that performs basic system health checks , like CPU usage and memory usage in percentage

### how to set it up
1. fork the repo
2. clone to local repo
3. run the script (bash server-stat.sh) - if you don't have bash intall it
